# apps/web

Landing site and project dashboard (W7). Own frontend environment, separate from `contracts/` and `apps/keeper/`.

Framework and indexer are not chosen yet. Record them in an NDR when W7 starts. See [`docs/nip/0000-the-roadmap.md`](../../docs/nip/0000-the-roadmap.md).
