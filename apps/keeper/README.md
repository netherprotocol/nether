# apps/keeper

Gravekeeper bot (W8). Own runtime, separate from `contracts/` and `apps/web/`.

Language and tooling are not chosen yet. Record them in an NDR when W8 starts. See [`docs/nip/0000-the-roadmap.md`](../../docs/nip/0000-the-roadmap.md).
