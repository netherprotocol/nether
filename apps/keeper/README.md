# apps/keeper

Gravekeeper bot (W8). Own runtime, separate from `contracts/` and `apps/web/`.

Plan: [`docs/nip/0009-grave-keeper.md`](../../docs/nip/0009-grave-keeper.md). Do not implement until that NIP is explicitly started.
